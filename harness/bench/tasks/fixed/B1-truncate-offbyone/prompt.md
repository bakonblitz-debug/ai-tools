Work in the Python project at __WORKDIR__ (do not touch anything outside it).

The test suite currently has a failure:

    python3 -m unittest discover -s tests

Follow bug-fix discipline: run the suite, read the failure, identify the root
cause in the source, and apply the minimal fix. Do not change any test file —
the tests define the correct behavior.

When finished, write the root cause and your fix to __WORKDIR__/ANSWER.md.
